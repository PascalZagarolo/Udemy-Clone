/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            "utfs.io",
            "upload.wikimedia.org",
            "logoipsum.com"
        ]
    }
}

module.exports = nextConfig
