/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            "utfs.io",
            "upload.wikimedia.org"
        ]
    }
}

module.exports = nextConfig
